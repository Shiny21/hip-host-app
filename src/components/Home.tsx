import React from 'react';

const Home: React.FC = () => (
  <div>
    <h2>Host - Pluggable Micro-Frontends by Shivani</h2>
    <p>Open the links above to load microfrontends from the runtime config.</p>
  </div>
);

export default Home;
